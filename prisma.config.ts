// prisma.config.ts
import "dotenv/config";
import { defineConfig } from "prisma/config";

export default defineConfig({
  // Use default settings; schema path is inferred as prisma/schema.prisma
});
